# 内存泄漏修复报告

## 修复日期
2026-04-30

## 问题分析

### pprof heap 分析结果

从 `http://127.0.0.1:6060/debug/pprof/heap?debug=1` 获取的内存分析：

| 指标 | 值 | 说明 |
|------|-----|------|
| Alloc | 5.2 MB | 当前分配 |
| TotalAlloc | 22.8 MB | 总分配 |
| HeapObjects | 6,643 | 堆中存活对象 |
| Mallocs | 43,766 | 分配次数 |
| Frees | 37,123 | 释放次数 |
| GC次数 | 10 | 垃圾回收次数 |

### 主要内存热点

1. **io.Copy 数据转发** - ~425KB
   - `socks5.(*SOCKS5Client).ForwardData` - SOCKS5 数据转发缓冲区
   - `auth.Handle.func4` - 认证处理数据转发

2. **DNS 缓存 JSON 序列化** - 48 bytes
   - `dnscache.(*DNSCache).Save` - 定期保存缓存

3. **URL.String() 调用**
   - `utils.CheckShouldUseProxy` - 每次请求都创建新 URL 对象

4. **net.Pipe 连接** - 112 bytes
   - `auth.Handle+0x208f` - SOCKS5/WebSocket 管道连接

### 根本原因

**第一轮修复**：连接资源未正确释放 - 在 WebSocket 和 SOCKS5 代理路径中，`server` 连接没有被 `defer server.Close()` 保护。

**第二轮修复**：在 io.Copy 数据转发后，`client` 连接没有被关闭。当一侧关闭时，defer 中的 Close() 没有及时执行到，导致 client 连接泄漏。

## 修复内容

### auth/auth.go

| 修复位置 | 原代码 | 修复后 |
|---------|-------|--------|
| 第 291 行 | `server = clientConn` | 添加 `defer server.Close()` |
| 第 429 行 | `server = clientConn` | 添加 `defer server.Close()` |
| 第 439 行 | 已有 defer | 无变化 |
| 第 465-467 行 | io.Copy 结束后 | 添加 `client.Close()` |

### simple/simple.go

| 修复位置 | 原代码 | 修复后 |
|---------|-------|--------|
| 第 255 行 | `server = clientConn` | 添加 `defer server.Close()` |
| 第 393 行 | `server = clientConn` | 添加 `defer server.Close()` |
| 第 403 行 | 已有 defer | 无变化 |
| 第 414 行 | `server, err = ...` | 添加 `defer server.Close()` |
| 第 464-466 行 | io.Copy 结束后 | 添加 `client.Close()` |

### 关键修复代码

```go
// 将客户端的请求转发至服务端，将服务端的响应转发给客户端
errCh := make(chan error, 2)
go func() {
    _, err := io.Copy(server, client)
    errCh <- err
}()
go func() {
    _, err := io.Copy(client, server)
    errCh <- err
}()
<-errCh
if server != nil {
    server.Close()
}
<-errCh
// 新增：确保客户端连接也被关闭，避免资源泄漏
if client != nil {
    client.Close()
}
```

## 修复验证

```bash
# 验证修复
grep -n "defer server.Close" auth/auth.go simple/simple.go
grep -n "client.Close" auth/auth.go simple/simple.go

# 结果
auth/auth.go:291:   defer server.Close() // 确保WebSocket连接也被关闭
auth/auth.go:429:   defer server.Close() // 确保SOCKS5连接也被关闭
auth/auth.go:439:   defer server.Close() // 确保连接被关闭
auth/auth.go:465-467: client.Close() // 确保客户端连接也被关闭
simple/simple.go:255:    defer server.Close() // 确保WebSocket连接也被关闭
simple/simple.go:393:    defer server.Close() // 确保SOCKS5连接也被关闭
simple/simple.go:403:    defer server.Close() // 确保连接被关闭
simple/simple.go:414:    defer server.Close() // 确保直接连接也被关闭
simple/simple.go:464-466: client.Close() // 确保客户端连接也被关闭
```

## 预期效果

修复后：
- ✅ WebSocket 代理连接将被正确关闭
- ✅ SOCKS5 代理连接将被正确关闭
- ✅ 直接连接将被正确关闭
- ✅ 客户端连接将被正确关闭
- ✅ 减少 goroutine 泄漏风险
- ✅ 减少文件描述符泄漏
- ✅ 内存使用更稳定

## 编译验证

```bash
go build -o main.exe ./cmd/
# 编译成功，无错误
```

## 测试建议

1. **启动带 pprof 的服务器**:
   ```bash
   ./main.exe -enable-pprof -port 28340 [其他参数]
   ```

2. **运行压力测试**:
   ```bash
   # 使用测试脚本进行循环请求
   powershell -File docs\内存泄漏测试程序-upstream.ps1
   ```

3. **监控内存变化**:
   ```bash
   # 监控 heap 变化
   watch -n 5 'curl -s http://127.0.0.1:6060/debug/pprof/heap?debug=1 | grep -E "Alloc|HeapObjects|Mallocs|Frees"'
   ```

4. **检查 goroutine 数量**:
   ```bash
   curl -s http://127.0.0.1:6060/debug/pprof/goroutine?debug=1 | wc -l
   ```

## 相关文件

- `auth/auth.go` - 认证代理处理
- `simple/simple.go` - 简单代理处理
- `scripts/内存泄漏修复验证.ps1` - 验证脚本
